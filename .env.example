# SQLite database directory inside the backend container.
# This is mounted to the host under data/sqlite
DB_PATH=/var/lib/sqlite

# Application runtime data directory (uploads, device images, etc.).
# This is mounted to the host under data/app.
APP_DATA_PATH=/var/lib/enervigil-backend

# Public HTTP port exposed by nginx.
# Normally only used for redirecting to HTTPS.
HTTP_PORT=80

# Public HTTPS port exposed by nginx (main access to ENERVIGIL UI/API).
HTTPS_PORT=443